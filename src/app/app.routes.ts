import { Routes } from '@angular/router';

export const routes: Routes = [
  // Como estás usando AppComponent como contenedor principal de login y dashboard,
  // puedes dejar las rutas vacías o redirigidas si no usas componentes separados.
];